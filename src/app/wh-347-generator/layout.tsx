import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Free WH-347 Generator: Fill the Official Form Online (No Signup)",
  description:
    "Fill out form WH-347 free in your browser — automatic overtime splitting, official Rev. Jan 2025 form, California DIR eCPR XML export. No signup, nothing uploaded.",
  alternates: { canonical: "https://www.wh347form.com/wh-347-generator" },
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
